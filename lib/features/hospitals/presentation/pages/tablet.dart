import 'package:flutter/material.dart';

import '../../domain/entities/hospital.dart';

class HospitalDetailsTabletPage extends StatelessWidget {
  const HospitalDetailsTabletPage({super.key, required this.entity});
  final HospitalEntity entity;

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
